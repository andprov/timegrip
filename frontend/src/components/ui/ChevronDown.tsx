/** Dropdown-trigger caret, factored out so its size stays consistent everywhere it's used. */
export function ChevronDown() {
  return <span className="text-sm text-gray-400 dark:text-gray-500">▾</span>
}
